<h1>I'M HUNGRY</h1>

<a href="<%= new_restaurant_path %>">Add a restaurant</a>

<% if @restaurants.any? %>
	<% @restaurants.each do |restaurant| %>
		<h2> <%= restaurant.name %>: <%= restaurant.description %></h2>
	<% end %>
<% else %>
	No restaurants yet
<% end %>
